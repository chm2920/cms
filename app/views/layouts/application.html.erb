<!DOCTYPE html>
<html>
<head>
  <title><% if !@title.nil? %><%= @title %> - <% else %><% end %>新闻大观</title>
  <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon">
  <meta name="keywords" content="<%= @keywords %>">
  <meta name="description" content="<%= @desc %>">
  <%= stylesheet_link_tag    "main", :media => "all" %>
  <%= csrf_meta_tags %>
</head>
<body>
<div id="Container">
	<div id="Header">
		<div class="header-top">
			<div class="wapper">申明：本站信息来自于互联网，如有版权争议，请联系QQ：395693519。</div>
		</div>
		<div class="header-body">
			<div class="wapper">
				<div class="logo"><a href="/">新闻大观</a></div>
				<ul class="nav">
					<% @catalogs.each do |catalog| %>
					<li><a href="<%= catalog.show_url %>"><%= catalog.name %></a></li>
					<% end %>
				</ul>
			</div>
		</div>
	</div>
	<div id="PageBody" class="wapper">
		<%= yield %>
	</div>
	<div id="Footer">
		<div class="foot wapper clearfix">
			<p>
				<% @catalogs.each_with_index do |catalog, i| %>
					<a href="<%= catalog.show_url %>"><%= catalog.name %></a>
					<% if (i + 1) % 7 != 0 %><i>|</i><% else %></p><p><% end %>
				<% end %>
			新闻大观 news.nxyouxi.com ©2013  All rights reserved.</p>
		</div>
	</div>
</div>
</body>
</html>
